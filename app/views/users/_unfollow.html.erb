<%= form_for(current_user.following_relationships.find_by(following_id: user.id), method: :delete, remote: true) do |f| %>
  	<%= f.submit "フォロー外す", class: "btn btn-large btn-primary follow-btn" %>
 <% end %>