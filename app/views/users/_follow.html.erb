<%= form_for(current_user.following_relationships.build(following_id: user.id), method: :post, remote: true) do |f| %>
	<%= f.hidden_field :following_id %>
	<%= f.submit "フォローする", class: "btn btn-large btn-success follow-btn"%>
<% end %>
